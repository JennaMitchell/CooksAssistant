import { CommonModule } from '@angular/common';
import { NgModule } from '@angular/core';
import { BackgroundChangerPopupComponent } from './background-changer.component';
@NgModule({
  declarations: [BackgroundChangerPopupComponent],

  imports: [CommonModule],
  exports: [BackgroundChangerPopupComponent],
})
export class BackgroundChangerPopupModule {}
